import React from "react";
import CreateUserSheet from "./CreateUserSheet";

export default {
  title: "Modals/CreateUserSheet",
  component: CreateUserSheet,
};

const Template = (args) => <CreateUserSheet {...args} />;

export const Default = Template.bind({});
