import React from "react";
import EditProfileSheet from "./EditProfileSheet";

export default {
  title: "Modals/EditProfileSheet",
  component: EditProfileSheet,
};

const Template = (args) => <EditProfileSheet {...args} />;

export const Default = Template.bind({});
