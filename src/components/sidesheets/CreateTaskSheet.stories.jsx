import React from "react";
import CreateTaskSheet from "./CreateTaskSheet";

export default {
  title: "Modals/CreateTaskSheet",
  component: CreateTaskSheet,
};

const Template = (args) => <CreateTaskSheet {...args} />;

export const Default = Template.bind({});
